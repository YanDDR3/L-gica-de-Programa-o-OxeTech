#include <stdio.h>

int main(){

    char letra;
    int numero;
    float real1;
    double real2;

    printf("%d bytes \n", sizeof(letra));
    printf("%d bytes \n", sizeof(numero));
    printf("%d bytes \n", sizeof(real1));
    printf("%d bytes \n", sizeof(real2));
    return 0;
}